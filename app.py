import streamlit as st
import pandas as pd

st.set_page_config(page_title="Material Calc Pro", layout="centered")

st.title("🏗️ คำนวณปริมาณวัสดุ")

@st.cache_data
def load_data():
    file_name = "เทสตาราง.xlsx - data ห้ามลบ ห้ามทำชีทนี้.csv"
    
    # รายชื่อรหัสภาษาที่ต้องสุ่มตรวจ
    encodings = ['cp874', 'tis-620', 'utf-8-sig', 'utf-8']
    
    for enc in encodings:
        try:
            # แก้ไข Error Tokenizing: ใช้ on_bad_lines เพื่อข้ามบรรทัดที่มีจำนวนคอลัมน์ไม่เท่ากัน
            # และใช้ engine='python' เพื่อความเสถียรในการอ่านไฟล์ที่มีภาษาไทย
            df = pd.read_csv(
                file_name, 
                skiprows=2, 
                header=None, 
                encoding=enc, 
                on_bad_lines='skip', 
                engine='python'
            )
            return df
        except:
            continue
    return None

try:
    df = load_data()
    
    if df is not None:
        # ดึงรายชื่อประเภทงาน (คอลัมน์แรก) และลบค่าว่างออก
        work_list = df[0].dropna().unique().tolist()
        selected_work = st.selectbox("📌 เลือกประเภทงานก่อสร้าง:", work_list)

        # ช่องกรอกปริมาณงาน
        quantity = st.number_input("🔢 ระบุปริมาณงาน (หน่วย):", min_value=0.0, step=1.0, format="%.2f")

        if quantity > 0:
            st.divider()
            st.subheader("📋 รายการที่ต้องใช้")
            
            # ดึงแถวที่เลือก
            selected_row = df[df[0] == selected_work].iloc[0]
            
            # จับคู่ชื่อวัสดุกับคอลัมน์อัตรา (Column 2, 4, 6, 8, 10)
            materials = {
                "หินใหญ่ (ลบ.ม.)": 2,
                "หินย่อย (ลบ.ม.)": 4,
                "ทรายหยาบ (ลบ.ม.)": 6,
                "ปูนซีเมนต์ (ถุง)": 8,
                "หินคลุก (ลบ.ม.)": 10
            }

            for name, idx in materials.items():
                try:
                    # ตรวจสอบตำแหน่งคอลัมน์ว่ามีอยู่จริงไหม
                    if idx < len(selected_row):
                        rate = selected_row[idx]
                        rate_val = float(rate)
                        if rate_val > 0:
                            total = quantity * rate_val
                            st.info(f"**{name}** \n\n จำนวน: **{total:,.2f}** (อัตรา: {rate_val})")
                except:
                    continue
    else:
        st.error("ไม่สามารถอ่านไฟล์ได้ กรุณาตรวจสอบการตั้งค่าไฟล์ CSV")

except Exception as e:
    st.error(f"เกิดข้อผิดพลาด: {e}")
